<h1>Edit trip from: <%= @trip.start_station.name %></h1>
<p>* required fields</p>

<%= render 'form', trip: @trip, user: current_user %>

<%= link_to "Back to trip from #{@trip.start_station.name}" , trip_path(@trip) %> |
