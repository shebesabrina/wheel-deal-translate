<br><br><br>
<h1><%= interpret("Stations Index") %></h1>

<% @stations.each do |station| %>
  <ul>
    <li><%= interpret("Name") %>: <%= link_to "#{station.name}", "/station/#{station.name}" %>
    </li>
    <li><%= interpret("Dock Count") %>: <%= station.dock_count %></li>
    <li><%= interpret("City") %>: <%= station.city %></li>
    <li><%= interpret("Installation Date") %>: <%= station.installation_date %></li>
    <% if !current_user.nil? && current_user.admin? %>
      <%= link_to 'Edit', edit_admin_station_path(station) %>
      <%= link_to "Delete", admin_station_path(station), method: "delete" %>
    <%end%>
  </ul><br>
<%end%>
